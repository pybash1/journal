---
const today = new Date();

const { dark } = Astro.props;
---

<footer
  class={`font-medium uppercase text-center pb-5 ${dark ? "bg-black text-white" : ""}`}
>
  &copy; {today.getFullYear()} PyBash
</footer>
