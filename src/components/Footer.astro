---
const today = new Date();

const { dark } = Astro.props;
---

<footer
  class={`font-medium uppercase text-center pb-5 font-mono ${dark ? "bg-dark text-heading" : ""}`}
>
  &copy; {today.getFullYear()} PyBash
</footer>
